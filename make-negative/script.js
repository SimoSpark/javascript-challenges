//make a given number to negative but my be the number is already negative
function makeNegative(num){
    if (num <= 0 ) return num
    else return -num
}
console.log(makeNegative(0))
//we can also answering like this :
//function makeNegative(num){
  //return num <0 ? num : -num; }

